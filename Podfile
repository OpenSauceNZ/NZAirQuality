# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!

pod "FSLineChart"
pod "TaniwhaTextField"
pod 'Material', '~> 2.0'

target 'NZAirQuality' do
  use_frameworks!

  target 'NZAirQualityTests' do
    inherit! :search_paths
  end

end
